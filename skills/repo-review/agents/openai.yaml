interface:
  display_name: "Repo Review"
  short_description: "Review a repository with file-backed findings"
  default_prompt: "Use $repo-review for the requested task."
policy:
  allow_implicit_invocation: true
