interface:
  display_name: "Unslop"
  short_description: "Remove AI tells from prose drafts"
  default_prompt: "Use $unslop to revise this prose so it reads naturally and preserves the facts."
policy:
  allow_implicit_invocation: false
